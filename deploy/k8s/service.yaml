apiVersion: v1
kind: Service
metadata:
  name: kovael-orchestrator
  labels:
    app.kubernetes.io/name: kovael-orchestrator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kovael-orchestrator
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
